# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build @multivio/auth-service`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t multivio-auth-service`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system multivio-auth-service && \
          adduser --system -G multivio-auth-service multivio-auth-service

COPY apps/auth-service/dist multivio-auth-service/
COPY apps/auth-service/package.json multivio-auth-service/
RUN chown -R multivio-auth-service:multivio-auth-service .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix multivio-auth-service --omit=dev -f install

CMD [ "node", "multivio-auth-service" ]
